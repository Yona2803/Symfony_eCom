<!-- filepath: /D:/VS.Code/MySymfony/Symfony_eCom/templates/items/dashBoard.html.twig -->
<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Admin Dashboard</title>
		<link rel="stylesheet" href="{{ asset('css/dashBoardStyle.css') }}">
        <link rel="icon" type="image/x-icon" href="{{ asset('img/icon/Icon.png') }}">
	</head>
	<body>
		<div
			class="dashboard">

			{% for label, messages in app.flashes %}
                {% for message in messages %}
                    <script>
                        alert('{{ message }}');
                    </script>
                {% endfor %}
            {% endfor %}

			<div class="sidebar">
				<h2>Dashboard</h2>
				<ul class="nav">
					<li>
						<a href="home">Home</a>
					</li>
					<li>
						<a href="#" onclick="loadContent('{{ path('wishlistPage') }}', 'main-content')">Wishlist</a>
					</li>
					<li>
						<a href="#users">Users</a>
						<ul class="submenu">
							<li>
								<a href="#addUser">Add User</a>
							</li>
							<li>
								<a href="#updateUser">Update User</a>
							</li>
						</ul>
					</li>
					<li>
						<a href="#">Products</a>
						<ul class="submenu">
							<li>
								<a href="#" onclick="loadContent('{{ path('add_item_page') }}', 'main-content')">Add Product</a>
							</li>
							<li>
								<a href="#updateProduct">Update Product</a>
							</li>
						</ul>
					</li>
					<li>
						<a href="#orders">Orders</a>
						<ul class="submenu">
							<li>
								<a href="#viewOrders">View Orders</a>
							</li>
							<li>
								<a href="#manageOrders">Manage Orders</a>
							</li>
						</ul>
					</li>
					<li>
						<a href="#settings">Settings</a>
						<ul class="submenu">
							<li>
								<a href="#generalSettings">General Settings</a>
							</li>
							<li>
								<a href="#securitySettings">Security Settings</a>
							</li>
						</ul>
					</li>
					<li>
						<a href="#logout">Logout</a>
					</li>
				</ul>
			</div>
			<div class="main-content" id="main-content">
				<header>
					<h1>Welcome, Admin</h1>
				</header>
				<section id="dashboard">
					<h2>Dashboard</h2>
					<p>Overview of the system.</p>
				</section>
				<section id="users">
					<h2>Users</h2>
					<p>Manage users here.</p>
				</section>
				<section id="products">
					<h2>Products</h2>
					<p>Manage products here.</p>
				</section>
				<section id="orders">
					<h2>Orders</h2>
					<p>Manage orders here.</p>
				</section>
				<section id="settings">
					<h2>Settings</h2>
					<p>Manage settings here.</p>
				</section>
			</div>
		</div>
		<script>
			function loadContent(url, containerId) {
                fetch(url).then(response => response.text()).then(html => {
                document.getElementById(containerId).innerHTML = html;
                }).catch(error => {
                console.error('Error loading content:', error);
                });
                }

                function previewImage(event) {
                const input = event.target;
                const reader = new FileReader();
                reader.onload = function () {
                const preview = document.getElementById('imagePreview');
                preview.src = reader.result;
                preview.style.display = 'block';
                };
                reader.readAsDataURL(input.files[0]);
                }
		</script>
        <script src="{{ asset('Pages/WishlistPage/main.js') }}"></script>
	</body>
</html>
